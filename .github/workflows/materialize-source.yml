name: Vollstaendigen 1.7.3 Quellstand herstellen

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  materialize:
    runs-on: windows-latest
    steps:
      - run: echo "Direktimport aktiv; kein Bootstrap-Import mehr."
